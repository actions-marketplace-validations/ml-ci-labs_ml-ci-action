version: 1
policy:
  regression_test: threshold
  regression_tolerance: 0.02
  metrics:
    accuracy:
      tolerance: 0.01
      direction: higher
      severity: fail
    loss:
      direction: lower
      severity: warn
